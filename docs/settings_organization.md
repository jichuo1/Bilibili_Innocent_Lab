# 设置界面的用途与区域分类

2026-09-05 起，原有混合的进阶设置拆为「净化进阶设置」和「增强进阶设置」。两者分别放在
「净化」与「增强」主栏的末尾。本文记录界面归属；偏好键、默认值、备份 ID、Remote 配置
及宿主 Hook 的行为保持不变。

同日进一步将「实验性功能」整理为固定主栏，下设独立展开的「外观」和「兼容」。原显示设置
卡片中的语言与桌面图标选项一并移入外观。

## 分类规则

- 净化：移除广告、运营引导、指定内容、界面组件或提示。辅助过滤判断的选项随过滤功能归类。
- 增强：增加阅读能力、改变浏览或评论交互、调整播放和显示方式。
- 依赖项放在一起：「保留大会员卡片位置」仍和隐藏卡片放在「我的页」；「匹配增强／强力模式」
  仍在相关推荐过滤面板。不能仅凭名称中的“增强”把过滤参数移入增强栏。
- 跨区域共享规则保留一份入口并标明范围。视频时长规则同时作用首页推荐和详情推荐，单列
  「首页与详情页共享过滤」，避免重复入口或误认成只作用某一页面。

## 净化主栏与进阶菜单

主栏保留视频详细页广告、视频暂停页广告和首页大卡轮播等既有快捷开关。主栏末尾是默认收起的
「净化进阶设置」；其中八个页面区域独立折叠，共享过滤保留直接可见的小标题和配置入口。

| 区域 | 功能与配套配置 |
| --- | --- |
| 首页推荐与布局 | 隐藏首页游戏中心、搜索推荐词；自定义首页标签及组件；过滤伪装信息流广告、普通推荐广告、图文、游戏推广、直播、课堂、大卡；推荐标题关键词开关和关键词规则 |
| 底栏与动态页 | 自定义底栏隐藏规则与扫描选择器；隐藏动态页同城、校园标签；动态内容过滤（关键词、发布者、带货与推荐附加卡、未解锁充电专属、话题栏、顶部 UP 栏直播条目） |
| 搜索 | 隐藏搜索结果推广卡；搜索结果标题关键词过滤及规则；按发布者过滤搜索结果及规则 |
| 我的页 | 隐藏大会员卡片、保留其位置；自定义组件隐藏规则与扫描选择器 |
| 播放页与竖屏流 | 隐藏竖屏切换按钮、播放器互动组件、影视自动活动半屏；竖屏内容过滤面板；相关推荐过滤面板 |
| 评论区 | 隐藏整个评论区；关闭关键词搜索链接；隐藏空评论引导、投票、关注按钮、体验反馈、运营推广；评论关键词过滤及规则；按用户等级过滤及最低等级；隐藏只有 @ 的评论；按发布者过滤及规则 |
| 启动与提示 | 开屏广告净化、青少年模式提醒、客户端更新提醒 |
| 分享 | 净化分享链接、小程序卡片转为普通链接 |
| 首页与详情页共享过滤 | 推荐视频最短／最长时长，同一规则同时作用首页与视频详情推荐 |

「播放页与竖屏流」同时承担弹幕内容流净化（按权重过滤、屏蔽大会员渐变彩色弹幕）。它与
播放器互动组件是两条独立链路：互动组件走 `ViewProgressReply`／`DmViewReply`，弹幕内容走
`DmSegMobileReply`，因此各自独立开关、独立诊断。

「评论区」的四条判据（关键词、用户等级、@ 整条、发布者）共用同一批列表 Hook，界面上也保持
相邻，不拆到别的区域。

动态内容过滤跟随原有的动态页签净化留在「底栏与动态页」，不另开区域；「搜索」是新增区域，
按页面进入顺序排在「底栏与动态页」之后、「我的页」之前。搜索框推荐词**没有单独开关**：它由
增强栏之外的既有开关「隐藏首页搜索推荐词」统一负责，该开关现在同时清掉协议层的默认词，
因此搜索页里也不再出现推荐词。

「播放页与竖屏流」的两个内容过滤面板保留完整的内部能力：

- 竖屏内容过滤：主页竖屏推荐，以及竖屏流中的广告、直播、游戏、番剧影视、课程、短剧、带货、
  电影、纪录片、电视剧、综艺、音乐。面板中已有的范围说明和类别联动保持原逻辑。
- 相关推荐过滤：商业推广、游戏、直播、课程、特殊推荐，以及匹配增强、强力模式、推荐理由过滤
  和关键词。其最终作用是过滤内容，因此整个面板归于净化。

2026-09-06 起，「首页推荐与布局」另增加「首页推荐过滤」勾选面板，放在标题关键词过滤开关
与关键词编辑入口之前。面板整合原有六项：推荐广告、推荐图文、游戏推广、直播、课堂、大屏
推荐；主菜单只显示一个带已选数量的入口。伪装信息流广告开关、标题关键词和竖屏过滤各自
保持原位置及语义，不并入这六项。

面板复用统一弹窗背景、入退场和返回手势，提供全选／清空、取消和保存。勾选只修改草稿，
保存时使用同一 editor 写入六个原偏好键中实际变化的项，取消不写配置。六项原名称继续进入
入口的搜索详情，搜索仍展开「净化进阶设置 → 首页推荐与布局」并定位到入口。
入口不另加水平 padding，沿用 SettingsMenuSpacing 的分组留白。没有配置键迁移、默认值
变化或 SettingsCatalog／Remote schema 变更。

## 增强主栏与进阶菜单

「自由复制」改为增强主栏中的小标题，评论复制和简介复制两个开关保持原行为。其后的
「复制气泡外观」集中自动跟随亮暗色、手动亮色两个选项；自动跟随从实验性功能移到这里，
原有覆盖关系、二次确认和提示更新逻辑保留。

增强主栏末尾是默认收起的「增强进阶设置」。首页与动态页、视频播放页、直播间、评论阅读与
交互、数字显示、启动与系统六个区域与净化使用同一套独立折叠卡片，默认收起，可以同时展开。

| 区域 | 功能 |
| --- | --- |
| 首页与动态页 | 竖屏视频进入详情页；动态页默认显示视频标签 |
| 视频播放页 | 默认播放画质；播放器状态栏透明；播放器能力（后台播放、小窗、投屏）；播放速度（禁用长按加速、自定义长按倍速、默认播放倍速） |
| 直播间 | 禁止上下滑动切换直播间；双击暂停而不是点赞 |
| 评论阅读与交互 | 回复脉络图；关闭评论正文／卡片点击快速回复 |
| 数字显示（全局） | 显示完整播放量、点赞量等数字；视频号显示为 AV 号 |
| 启动与系统 | 开屏页跟随深色模式；使用系统媒体控制通知；站外链接用系统浏览器打开 |

区域按页面归类、全局项排在末尾：直播间是独立页面所以紧跟视频播放页；AV 号显示是"同一个
数字换一种写法"，与完整播放量同属数字显示；开屏、系统通知和站外跳转都发生在页面之外，
统一收进「启动与系统」，不再各自新开区域。

新增区域时，`MainActivity.AdvancedSettingsCategory` 的枚举顺序必须与 Hikage 树里 marker 出现
的顺序完全一致——`AdvancedCategoryLayoutPolicy.resolve` 要求 markerIndices 严格递增且首个
为 0，顺序不一致会让整个进阶菜单退回未分区的原始布局。

2026-09-06 的播放器移植沿用「视频播放页」原有区域，只增加「播放器能力」「播放速度」两个
小标题，不增加分类 marker 或重复水平边距。六项名称均由现有 View 树搜索索引收录，继续使用
「增强进阶设置 → 视频播放页」展开链。倍速弹窗沿用统一模态背景、动画与预测性返回，支持
0.25～4 倍、最多两位小数，另有明确的「跟随哔哩哔哩」恢复入口；非法输入保持弹窗等待修正。
禁用长按加速优先，但长按倍速仍可编辑，摘要说明其暂不生效，关闭禁用开关即可恢复保存值。
六项均默认关闭／跟随宿主，加入 SettingsCatalog v13、备份预览和 Remote 白名单，旧备份缺失项
保留当前值。播放器能力只尝试开放客户端控件，不修改付费权限或视频来源；默认倍速只初始化
管理器基础速度，长按临时速度及后续手动切换由宿主维护。详见 [播放器移植说明](player_controls_port.md)。

「关闭点击快速回复」改变正常评论交互方式，归于增强；「关闭评论关键词搜索链接」去掉搜索
导流入口，归于净化。它们继续复用各自已有安装器，不因界面归属而拆分或改写 Hook。

## 实验性功能：外观与兼容

实验性功能不再整体折叠后再找内部选项。两个二级菜单并列、默认收起，可以同时展开。

| 二级菜单 | 小分组与内容 |
| --- | --- |
| 外观 → 风格与配色 | 界面美学、Liquid Glass 背景、SPEC 2021／2025 开关 |
| 外观 → 显示设置 | 应用语言、隐藏桌面图标及原有提示 |
| 兼容 | 免 Root 支持、漫游版本支持扩展、预见式返回动画、重新适配当前版本 |

预见式返回继续在 Android 16 及以上整组隐藏，已有偏好及平台判断不变。更换皮肤／SPEC 的
保存与界面重建、背景图片处理、语言切换、桌面图标显隐、兼容配置发布及适配确认沿用原逻辑。

外观内容容器统一提供水平边距，入口行不再额外叠加横向 padding；入口标题与开关标题使用同一
文字起点和行右边界。两个进阶菜单分别沿用净化星形与增强魔棒图标，便于与所属主栏对应。
增强魔棒、外观调色盘、兼容拼图来自 Google Material Design Icons，使用本地 24×24 矢量资源
和已有主题着色；来源与 Apache-2.0 许可证记录于 `THIRD_PARTY_NOTICES.md`。

## 布局、搜索与兼容边界

组内小标题使用统一的 AdvancedSubsectionStyle：14sp 加粗、完整不透明度的主题强调色，
上边距 20dp、下边距 6dp，不增加水平 padding。仅作用于「推荐内容」「动态页」「动态内容
过滤」「提示页面」「更新提醒」「播放器能力」「播放速度」七处，不调整分类折叠标题、
普通开关、说明文本或主栏标题。小标题允许换行并标记为无障碍标题，不新增点击或折叠行为。
扫描提示直接写明打开首页／主界面／“我的”页以扫描勾选项，保留手动规则、最少保留项、
重启及扫描过期／缺失提示。

两个进阶入口在构建 View 树时直接成为各自主栏的最后一个子项，旧的“移动到实验性功能之后”
逻辑已移除。首帧前只整理区域内既有 View，不复制功能控件、不重绑监听器。

区域分界只按标题位置切分。下一个标题前的最后一个控件／说明属于当前区域，不能继续使用
旧布局“减一跳过分隔线”的假设。输入不完整时保留原始布局；单测验证不同区域数量时，所有
非标题子项恰好被分入一个区域。

搜索结果显示「用途进阶设置 · 作用区域」路径，能索引收起的区域。选择结果时展开正确的进阶
菜单及所需区域，再滚动并高亮；另一个用途菜单的展开状态不变。全部展开／收起继续只动画
alpha、translationY 和箭头 rotation，不逐帧修改高度或创建超长硬件层。
外观与兼容也走同一菜单状态与动画入口；搜索能索引收起的内容，并在命中菜单标题时直接展开
该菜单。Activity 销毁时清空四个菜单的 View 引用并取消动画。

静态重排审计确认：配置常量引用的集合及出现次数一致；62 段实际开关回调正文除缩进外完全
一致。SettingsCatalog 仍为 10（86 项），VersionAdapter 仍为 schema 53 / rule 47。
本次不涉及配置协议迁移、宿主适配规则或框架入口变更。

> 2026-09-06 更新：哔哩漫游移植两批完成后 SettingsCatalog 为 12（113 项），VersionAdapter 为
> schema 54 / rule 49（第二批全是 bapis 未混淆类，安装期结构定位，未再动 schema）。上面这段
> 是重排当轮的事实快照，保留原貌。

## 净化／增强重排阶段的验证结果

- 完整本地门禁：`assembleDebug testDebugUnitTest lintDebug minifyReleaseWithR8 --no-daemon` 成功。
  121 个套件、675 项 JVM 测试零失败；Lint 0 Error / 170 Warning。
- 设备：Android 16 / API 36，在原有已授权配置下覆盖安装同签名 Debug APK，保留应用数据。
  安装后 APK SHA-256 与本地一致：
  `cd47ddf0cd9d56c0dbd9b2c5884253d5e2801ee98b0bdbe1be0ef8369faef207`。
- 两项真实界面测试均通过（12.547s）：主栏归属和进阶入口的末尾位置；两个菜单独立展开；
  快速反复切换后的可见性；搜索从增强评论入口切到净化“我的页”时展开正确菜单与区域；
  全部 86 项可备份配置在菜单／搜索操作前后保持一致。
- 已检查中文深色界面的净化进阶、增强主栏、增强进阶及搜索定位截图。英文、简中、繁中的
  资源键一致，主界面资源引用完整。本轮未切换全部皮肤或其他框架，也未重测 B 站功能 Hook。
- Debug APK 签名、全部 19 个 DEX、Modern 入口与 ZIP 路径安全检查通过。Release 只校验 R8，
  未生成或发布签名 Release。

测试环境的两处修正单独记录：第一次设备息屏导致等待；窗口仅在测试期间添加 KEEP_SCREEN_ON。
首次搜索测试把同文案输入框与结果一起匹配，已明确排除 EditText 后重测通过。这两处均未改变
生产界面逻辑、用户锁屏设置或功能开关。

本地证据位于 `Temp/settings-reorganization-20260905/`：构建日志、原始回调比对快照、安装前
APK、最终 APK 验证 JSON、`instrumentation-final.log` 和 `screenshots/`。临时测试包已清理；
设备保留本次更新的模块。

## 实验性功能与对齐修正的最终验证（2026-09-05）

完整门禁 `assembleDebug testDebugUnitTest lintDebug minifyReleaseWithR8 assembleDebugAndroidTest
--console=plain --no-daemon` 通过（5m50s）：121 suites / 675 JVM tests，0 failure/error；
Lint 0 Error / 170 Warning。三语言资源、矢量 XML 和全部设置回调的静态核对通过。

Android 16/API 36 真机三项用例通过（19.253s）：原净化／增强回归、外观／兼容独立展开与搜索、
五个外观标题的文字起点及行右边界一致；全部 86 项配置、应用语言标签和桌面图标启用状态均保持。
已检查最终外观、显示子组、净化进阶与增强进阶截图，两个进阶图标与所属主栏一致。

第一次旧搜索用例受框架更新悬浮通知遮挡顶部搜索按钮影响，截图保留了遮挡证据；后续在通知
消失后重测，并把输入框查询明确限制在 Dialog 根。未关闭系统通知、改变锁屏或触碰功能开关。

最终 Debug APK 为 1.1.3/14，12,874,890 bytes，SHA-256：
`a54a6c07df0d19f88ea95351ec2a71c43ad2e71cc818184d8f5b935ac9c70cc6`。设备安装后摘要一致。签名仍为
`838043535961255716e3f49e6ceebbcadaae1337e2693bec0ec77e1ecec57a52`；19 个 DEX、Modern
101/102 入口、ZIP 安全与源码时间核对通过。Release 只验证 R8。

本轮证据位于 `Temp/experimental-appearance-20260905/`，最终日志为 `verification-aligned.log`
及 `instrumentation-aligned.log`，截图为该目录的 `screenshots/`。临时测试包已清理，设备保留
已更新的模块；本轮没有新增宿主 Hook 或跨框架运行验收。

## 功能开关菜单的统一水平留白（2026-09-05 补记）

净化进阶分组内的开关是基准：菜单壳 8dp，加上分组内容 12dp，控件距离二级菜单边框合计
20dp。增强进阶以前只有菜单壳的 8dp，兼容以前只有 10dp，因此视觉上明显贴边。

当前由 `SettingsMenuSpacing` 统一维护：增强和净化使用同一分组内容容器提供 12dp，增强
进阶根不再重复加 padding；兼容保留原 10dp 菜单壳，
在内容根补足剩余距离。兼容使用已经换算的菜单壳像素值计算差额，避免不同屏幕密度下各层
取整后出现 1px 偏差。原净化开关的位置保持不变。默认画质和重新适配入口取消额外水平缩进，
跟随所在内容容器；分组小标题也随增强内容根一起内收。

新增同类功能菜单必须复用这份规则。不要给每个开关单独补 padding，也不要全局修改
MaterialSwitch；不要把“相同内层 padding”误认为“相同最终留白”。带前导图标的操作行以整行
边框对齐，文本自然位于图标后方。外观已有的标题对齐保持原规则。

兼容菜单标题按用户要求简化为「免 Root 支持」，NPatch 的说明保留在正文。不要为了长标题
削小统一留白；真机回归同时检查该标题没有省略号。

真机回归直接比较净化、增强、兼容三处控件相对菜单外框的左右距离，覆盖增强所有七个入口、
兼容的免 Root／漫游，以及支持时的预见式返回。这样平铺或折叠层级变化后也能检测边距漂移。


### 增强区域折叠的后续调整

按用户要求，增强的四个区域已从平铺小标题改为与净化一致的折叠卡片。每组有标题、强调色标记、
箭头和独立展开状态。展开一组不关闭其他组；收起并重新打开增强进阶时，子组状态仍然保留。
搜索命中某个增强功能时会同时展开增强进阶和所属区域。

12dp 内容留白下沉到共享分组构建器，不能保留此前平铺根的 12dp 再叠加，否则会得到 32dp
错误总留白。跨菜单几何回归会先展开各组再比较，继续锁定与净化相同的 20dp 基准。

### 本轮最终验证结果（统一留白、短标题、增强分组折叠）

完整门禁通过：assembleDebug / testDebugUnitTest / lintDebug / minifyReleaseWithR8 /
assembleDebugAndroidTest，统一 --no-daemon（8m19s）。121 suites / 675 JVM tests 零失败；
Lint 0 Error / 170 Warning。最终 Android 16 真机四项用例通过（22.42s），覆盖增强四组默认
收起、独立展开、父级收放后的子组状态、跨层级搜索、跨菜单相同左右留白，以及短标题无省略号。
全部 86 项配置保持不变；语言和桌面图标状态回归同样通过。最终两张增强折叠／展开截图已检查。

最终 APK 1.1.3/14，12,874,818 bytes，SHA-256 `5ca02b157348918268b9187effb08f637f07ea27db696af16e914ed2489bf073`；
设备覆盖安装成功且摘要一致，签名仍为既有 Debug 证书。19 个 DEX、Modern 101/102 入口、ZIP
条目和源码时间检查通过。临时测试应用已清理，设备保留更新后的模块。未改变宿主 Hook 或框架配置。

证据目录：`Temp/menu-insets-20260905/`。最终日志 `verification-folded.log`、
`instrumentation-folded.log`；最终分组截图 `screenshots/enhancement-advanced.png`，展开与边距
截图 `screenshots/insets-enhancement.png` 和 `screenshots/insets-compatibility.png`。
