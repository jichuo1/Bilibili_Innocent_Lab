# 细分能力诊断与更新提示（2026-09-07）

## 界面补充

- GitHub 二级页的遥测条目不可整行点击，仅小 ⓘ 按钮可进入详情页。
- 开启摘要为“已开启 · 自动上传（Per 24h）”，简繁中文使用全角括号；详细频率仍在说明页完整保留。
- NEW 标记相对原 27dp 图标框向右、向上各偏移 5dp，文字主体尺寸保持小号，新增 3dp
  小尾角，复用 BubbleDrawable Path；放开局部裁切并增加仅可见时生效的点击代理。
  不改变 GitHub 图标尺寸、边距或工具栏占位。此处仍需真机外观与点击验证。
- 状态文本沿用气泡亮暗说明的 120ms 淡出 / 180ms 淡入；按实际文本宽度预留两态
  最大高度，避免换行差异使弹窗高度跳变。快速切换取消旧动画并恢复到最新状态。
- 本轮 Android 完整构建、832 项 JVM 测试及 Lint 通过；真机动画和后台上传另行验收。

## 版本变化上传补充

- 当前遥测告知升级至 5（下方告知 4 是此前交付记录）；旧开启用户重新确认，关闭用户保持关闭。
- 宿主主进程安装链完成且 Activity 首次 resumed 后，在独立后台线程通知模块；
  两项条件无论先后都可触发，后台服务单独唤醒不触发。Provider 不可达时使用显式广播，
  校验 PendingIntent 创建者 UID，API 34+ 同时校验实际发送者 UID。不接受调用方自报版本或载荷。
- 模块重新查询 nonce/摘要/版本来源均有效且安装链完成的回执，比较模块/宿主数值版本组合。
  首次观察到的组合也报一次；成功后持久化去重，任一版本变化（含降级）才再次触发。
- 独立滚动 24 小时最多 3 次网络尝试；失败计次，至少退避 15 分钟，等后续宿主启动再尝试。
  不更新手动或常规自动上传额度；删除报告、关闭再开启不清空版本额度。
- 不常驻、不轮询、不修改 Hook 授权链；后台限制、通知不可达或回执失效可能延后上传。
  Receiver 最多持有 8 秒；Provider 工作等待最多 20 秒，网络沿用 8 秒连接/读取超时。
- 服务端 `version_change` 只接受告知 5 + schema 2 的完整回执，使用独立额度表、服务端时间、
  双标识摘要限额、事务和总预算；刷新当日快照，不把重复报告算成新设备。
- `server/` 为本地忽略目录，不再随公开仓库发布。跨端目录投影测试在缺少服务端时明确跳过，
  Android 自身的目录完整性、设置映射与载荷上限测试不跳过。
- 真机宿主后台通知、不同 Android/框架环境以及实际版本升级触发仍需单独验收；构建不等于真机通过。

## 已交付

- 冷启动后，首次连续前台停留 10 秒触发一次更新检查；每进程只自动检查一次。
  暂停取消计时，返回重新计算连续停留；不再使用自动更新检查的 24 小时冷却。
- 当前渠道发现新版本后，仅在 GitHub 图标右上角展开 NEW 气泡，点击复用现有更新界面。
  手动检查保留反馈；后台不弹窗，过期渠道/请求结果不覆盖新结果，界面重建可接收进程内缓存结果。
- 能力目录包含 116 个叶能力/技术路径，映射现有 119 项设置或明确列为本地排除。
  目录与服务端投影有交叉校验，超限不能静默截断。
- 安装结果区分完整、部分、缺失、失败、不适用和未知；没有独立证据时不复制父级成功。
  父级结合子项与已知组级异常保守归纳，不重复计入细分成功率。
- 分别接入首页/Story/动态/搜索/评论/相关推荐/分享/直播/播放器/自由复制等路径。
  播放器互动层拆出独立字段路径；详情推广保留惰性 SDK 对象的待验证状态；
  首页轮播区分视图路径和推荐流路径。
- 运行证据按能力分级：35 项支持触发/应用，14 项只支持触发，67 项以安装证据为主。
  运行异常使用有界标志，不发送异常文本。不能把这些数字说成全部子功能已运行验证。
  第三方漫游兼容只有工作派发、没有独立安装回执时，明确 UNKNOWN。
- 宿主诊断协议 3、导出格式 5、遥测 schema 2、能力目录 1、独立遥测告知 4。
  核心 Hook 条款仍为 2；遥测原来关闭的仍关闭，旧授权不能发送扩展数据。
  告知明确说明细分状态可能间接反映已启用功能，不发送规则内容或具体设置值。
- 旧 schema 1 继续接收；看板按粒度分开，新默认细分能力，可切回旧大组。
  长期细分聚合使用新 capability_daily 表，不混入旧 feature_daily。
- 32 KiB 上报预算、自动间隔、手动滚动 24 小时 3 次、来源/总量限流和删除规则保持。
  分析超过 2000 个候选原始快照时要求收窄标量筛选，拒绝整个分析而非悄悄采样。

## 验证

- Android：143 suites / 823 tests，零失败/错误；assembleDebug、lintDebug、
  minifyReleaseWithR8 通过；Lint 0 errors / 174 warnings。
- 服务端：58 项测试通过；全目录载荷、未知/部分状态、父子隔离、告知门禁、
  查询预算、旧新聚合隔离、未知/不完整快照排除和限流均有回归。
- 两个线上环境：旧告知标记 400；自动 204；手动 204/204/204/429；
  耗尽手动额度后自动仍 204；七组查询成功，其中 features 返回全部 116 行；
  最后用本次随机删除令牌清理合成原始报告，purge 204。
- 匿名访问看板仍为 302；没有更改 Access 策略或新增付费依赖。
- 当前 APK 未安装到设备；NEW 动画、细分 Hook 运行和授权界面未做真机验收。
  本机合成 HTML 预览 200，不是浏览器截图/交互验收。
- Debug APK（Staging）13,411,306 bytes，SHA-256：
  d9a981b094389bb452c59c141a8ecf9f97ff69ea9b77fb01cd57c503fcfd1f1b

## 云端版本

- Staging：c9a79060-6687-4178-8ce3-f4774abf5dc9
- Production：9bc9b794-1af9-47af-9df9-1c7a56a75afb
- 私人看板：0348ecdc-6e51-4adc-9b14-39742acd273c
- 两环境已应用 0004_capability_daily.sql；仅新增表，未删除或回填旧记录。

## 保留的边界

- 注册成功不等于实际生效；clear 方法不抛异常不证明原来有内容。
- 运行证据表示当前宿主进程曾发生的阶段，不是当天操作次数或用户人数。
- 合成请求不是设备来源报告；本地构建不是跨版本 Hook 真机通过。
- 当前工作树还有其他会话的组件选择重扫改动，已保留，没有 reset、stash 或提交。
