# 播放器能力与播放速度移植

## 2026-09-08 更新：按视频会话应用默认倍速

本节覆盖下文历史版本中“只在构造时初始化一次”的限制。现有三个设置及其整数配置键不变，
仍然默认跟随宿主；不新增生产依赖或遥测字段。

- 基础速度、长按临时速度分离。新视频的基础速度使用模块默认值；同一 aid/cid 的重复绑定
  保留手动选择，临时槽位始终交宿主管理。2× 基础 + 3× 长按为 2→3→2，不相乘。
- Theseus 会话方法由稳定的 runPlayable 协程类反查 owner，再按结构定位；支持旧版
  Continuation 和新版 ContinuationImpl。回调读取实际绑定的 playable，不采用仍等待互斥锁
  的请求参数。只在确认有效 aid/cid 变化后更新基础 Flow，失败不提交会话标记。
- 管理器构造时识别基础字段，不在临时速度为 3× 时猜哪个字段是基础。构造已成功初始化后、
  首次绑定前发生的显式速度更改（例如共享播放器交接）保留，下一条媒体再恢复模块默认。
- 共用 MediaPlayContextImpl 登记准备监听器时，仅按 OnPreparedListener 和 IPlayerCoreService
  字段关系发现实际回调与调速方法；不枚举全量 DEX、不写死混淆类名。只在已知准备回调的
  同线程、同一核心、同一媒体、首个调速调用中替换初始速度。手动/长按等普通调速不拦截。
- 新版核心关联管理器后，准备阶段读取当前基础/临时合成速度，避免清晰度切换时吞掉临时
  加速；旧核心按共享播放上下文和媒体 ID 去重。同一上下文的播放器交接保留当前速度。
  媒体身份缺失、来源不明确或不在宿主点播来源常量范围时放行，直播、音乐、播客、任意
  URL 不强行覆盖。远端投屏和外部播放器不在本机速度控制覆盖承诺内。
- 状态只在内存中，播放器/核心/上下文均为弱引用键或弱引用值，不上传、不持久化媒体身份。
  嵌套准备回调隔离作用域，宿主抛错后恢复外层作用域，反射失败不冒泡到宿主播放逻辑。
- 诊断将实际准备回调视为延迟覆盖单位，未发现前报告 partial，不能因入口登记成功就报告
  全覆盖。APPLIED 要有速度修改和写后读回；不以注册回调冒充功能生效。

验证边界：结构匹配、JVM 替身测试和构建不代表所有页面已完成真机验收。实机仍需覆盖
普通视频、分 P/连播、番剧影视、Story、预览/内嵌播放、后台/小窗和清晰度切换；特别检查
禁用长按、长按自定义、手动改速、准备中断及共享交接的组合。逐版结构探针位于本地
Temp/player-port-20260906/probe_sessions.py，覆盖保留的 27 个 8.84.0–9.11.0 样本
（含 8.90.2）；没有 8.85.x 或更早版本的证据。

本轮参考哔哩漫游的功能意图，以本工程现有 Modern API 101/102、KavaRef 成员解析、
FeatureInstaller、Moss 回调代理、配置白名单和设置布局重新实现，不引入新的生产依赖。
功能默认关闭／跟随宿主；修改后需重启哔哩哔哩主进程。

## 功能与边界

| 设置 | 行为 | 不做的事 |
| --- | --- | --- |
| 后台播放 | 调整已有播放响应中后台播放能力的客户端禁用／支持标志 | 不生成播放地址，不解锁付费视频，不绕过系统后台限制 |
| 小窗播放 | 调整小窗能力，并开放 MINIPLAYER 菜单的两个已验证构造槽位 | 不全局修改其他菜单或悬浮窗权限 |
| 投屏 | 调整投屏能力的客户端禁用／支持标志 | 不修改设备发现、DRM、服务端授权或画质 |
| 禁用长按加速 | 只消费 TripleSpeed 业务监听器的长按开始回调 | 不 Hook 全局触摸，不拦松手清理及其他手势 |
| 自定义长按倍速 | 修改加速协程构造器捕获的 Float 参数，范围 0.25～4 倍 | 不改最终字段、不轮询速度；禁用加速时不安装此路径 |
| 默认播放倍速 | 在速度管理器构造完成后初始化基础 Flow | 不写临时 Flow，不持续覆盖用户手动选择，不承诺每次切视频都重新覆盖 |

GUI 位置为「增强 → 增强进阶设置 → 视频播放页」，分为「播放器能力」「播放速度」两个小组。
自定义倍速最多两位小数，另提供「跟随哔哩哔哩」。禁用长按时保留自定义值，并在摘要中明确
提示暂不生效。英文、简体、繁体资源同步；既有分类 marker 顺序与水平留白不变。

## 安装与热路径

- PlayerCapabilityFeatureInstaller 覆盖 playerunite / playurl 两族同步和异步响应，复用
  MossResponseHandlerProxy，原响应身份和错误／完成回调保持不变。
- PlayerCapabilityAccess 在安装期解析成员，按 newBuilder(original) 的真实返回类型定位
  builder，不写死混淆的内部类名。复制选中的 ArcConf 和容器，全部副本准备完成后才写顶层
  配置字段；默认回复、没有视频信息的回复放行，未选字段、未知字段和视频数据保留。
- 共享协议只写 2（投屏）、9（后台）、23（小窗）中已选项，不包含 36（听视频）。
  已开放的能力不复制，第二次经过响应边界是幂等的。
- PlayerSpeedLocator 在安装期核验稳定业务类、构造参数及字段类型；混淆的外部服务 owner
  和双 Flow 字段名不作为候选常量。8.x 有基础／合成两个 getter，9.x 内联后只有一个；
  只接受已验证的一至两个 Float getter，初始化前后整组读回。
- 仅在“两个 Flow 中唯一一个为 Float 1、另一个为 null，速度 getter 均为 1”的初始状态写入。
  写后 Flow 和 getter 必须全部回读为目标值，否则尽力恢复基础值，不上报 APPLIED。
- 默认配置不注册 Hook；回调不执行类扫描、文件 I/O、IPC、定时轮询或持久化，不缓存播放器、
  View、请求或响应实例。额外分配按响应／管理器／长按事件发生，不在逐帧更新中发生。
- 安装覆盖分母包含已启用但解析或注册失败的单位，缺失不会被隐藏成全覆盖。诊断中心增加
  player_capabilities 和 player_speed。ADAPTED、OBSERVED 和 APPLIED 含义保持分离；
  仅修改小窗构造参数不冒充已验证的模型改写。

## 配置兼容

六项新增配置进入 SettingsCatalog v13（119 项，118 项可自动恢复）；两个倍速用百分比整数
存储，0 为跟随宿主，不新增 Float 协议类型。Remote schema 仍为 4；发布全量新目录后宿主才
接受授权配置。旧备份中不存在的新项保留当前值，不将缺失视为 false 或 0。

升级模块后应先打开模块设置页，确认配置发布完成，再重启哔哩哔哩。旧目录与新宿主 Hook
不匹配时仍按既有授权协议全部拒绝，不增加私有文件或广播回退。

## 验证口径

2026-09-06 离线扫描 27 个真实 APK，覆盖 8.84.0～9.11.0 的本地样本以及从已连接设备复制的
8.90.2／8902100；每版 51 条成员／选择器／身份契约通过。没有 8.85.x 或 8.84.0 以前的样本，
不得外推支持。定位器对应的 builder 继承、唯一长按 Float 构造槽位、双 Flow 和 getter 数量
均逐版检查；跨 DEX 引用记录保留在本地报告中。

语义检查使用明确的命令行入口 jadx.cli.JadxCLI，不是 all.jar 默认的 GUI 入口。8.90.2
和 9.11.0 的速度管理器，以及长按监听器／捕获协程／注册链均已定点核对。JADX 输出并非
运行时证据；可能有反编译控制流警告，不能只凭重建 Java 的行顺序推断协程行为。

本地探针和原始报告在被忽略的 Temp/player-port-20260906/。单元测试涵盖配置边界、默认实例
隔离、未选能力／视频数据保留、幂等、同步／异步转发、安装失败分母、长按优先级、双 Flow
初始化和失败回滚；历史备份 fixture 保留，新增 v13 fixture。

离线结构和 JVM 测试不能替代真实框架注入、服务端控件可用性或 UI 真机验收。真机应分别检查：

1. 全部关闭时原播放、手势、小窗和投屏不受影响。
2. 三个能力逐项开启，检查对应控件；无权限／无视频信息回复和未选能力不变。
3. 默认 1.25 倍、长按 2.75 倍，松手恢复基础倍速；播放中手动调速仍可用。
4. 禁用长按时不进入加速，松手及单击／双击／进度拖动正常；关闭禁用后保存值仍在。
5. 跟随宿主、重启、旧备份预览／恢复、搜索跳转，以及深浅色与不同字号的弹窗显示。

本轮不升级设备宿主、不自动修改设备开关，也不以 APK 打包成功或结构匹配宣称上述真机行为通过。

### 本轮最终本地结果（2026-09-06）

- assembleDebug / testDebugUnitTest / lintDebug / minifyReleaseWithR8 四项门禁通过，统一
  --no-daemon；最终一轮 6m46s，包含播放器小组留白的最后调整。
- 136 suites / 774 tests，失败、错误和跳过均为 0；比本轮前新增 26 项测试。
- Lint 0 errors / 172 warnings；本轮三处 KavaRef 扩展建议已按项目规范修正，未抑制。
- API 101 真实接口二进制烟测通过：无 setId、同点单次原生注册、只读 proceed 通过；
  类加载日志没有 ModernHookIdsApi102。此项不是 Irena／Vector／NPatch 真机验收。
- Debug APK 为 1.1.3／14，minSdk 27 / targetSdk 37，arm64-v8a + armeabi-v7a，19 个 DEX，
  APK v2 签名验证通过，无绝对路径或目录穿越 ZIP 条目，产物晚于最终源码。
- APK SHA-256：1B1FA6395A700E48D0357B32A8F85A33543C84D7EA40516022562D29B9211C7E。

首次定向测试曾暴露目录初始化仍断言 113 项，已同步为 119 项并用历史 fixture 和全量门禁复验；
这不是旧备份格式损坏。实际设备 8.90.2 的服务构造注册链也经 JADX CLI 复查，确实启动本次
定位的 runOldTripleSpeed 链路。未安装最终 APK、未更改设备开关、未提交或推送。
