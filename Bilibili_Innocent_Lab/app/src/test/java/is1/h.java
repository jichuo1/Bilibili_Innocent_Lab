package is1;

/**
 * 9.11.0(9110400) 默认画质计算入口（es1.i 搬家到此）。
 *
 * <p>真机上这个类**只有** {@code static a()I} 一个方法，所以定位器的
 * "owner 内唯一无参 Int 入口"天然成立；夹具保持同样形状。
 */
public final class h {
    public static int a() {
        return 32;
    }
}
